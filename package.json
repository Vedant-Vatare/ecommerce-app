{
  "name": "ecommerce-app",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm --parallel --filter client --filter server dev",
    "dev:client": "pnpm --filter client dev",
    "dev:server": "pnpm --filter server dev",
    "build:client": "pnpm --filter client build",
    "start": "pnpm --parallel --filter client --filter server start",
    "start:client": "pnpm --filter client start",
    "start:server": "pnpm --filter server start",
    "format": "prettier --write .",
    "format:client": "prettier --write client",
    "format:server": "prettier --write server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "prettier": "^3.6.2",
    "prettier-plugin-tailwindcss": "^0.7.1"
  }
}
