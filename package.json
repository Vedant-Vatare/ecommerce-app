{
  "name": "ecommerce-app",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "dependencies": {
    "prettier": "^3.6.2"
  },
  "scripts": {
    "dev:client": "pnpm --filter client dev",
    "dev:server": "pnpm --filter server dev",
    "dev:both": "pnpm -r --parallel dev",
    "build:client": "pnpm --filter client build",
    "build:server": "pnpm --filter server build",
    "build:all": "pnpm -r build",
    "install:all": "pnpm install",
    "format:client": "pnpm prettier --write client",
    "format:server": "pnpm prettier --write server",
    "clean": "pnpm -r clean",
    "test": "pnpm -r test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module"
}
